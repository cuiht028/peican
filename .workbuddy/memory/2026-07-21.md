# 2026-07-21 配餐工作日志

## 川渝家常菜扩充（V1.1）

### 需求
扩充成渝区域家常菜谱，增加毛血旺、钵钵鸡、冒菜等家庭可做的川渝菜，以及可购买的卤菜、烤鸭、豆花等，粗粮注意应季优先。

### APK 打包进展
- 已检查 Flet APK 构建配置，入口和依赖结构完整。
- 本地语法检查通过，核心测试 40/40 通过。
- GitHub 仓库可访问，但 Actions 查询显示 workflow 数量为 0、运行数为 0；远端尚未有 `.github/workflows/build-apk.yml`，因此目前没有 APK 产物。
- 之前给出的 GitHub 新建文件链接路径格式错误，正确方式是打开仓库后点 `Add file` → `Create new file`，或使用 `https://github.com/cuiht028/peican/new/main?filename=.github%2Fworkflows%2Fbuild-apk.yml`；访问时需先登录 GitHub。

### 实际改动

**seed_ingredients.py（+28 种新食材）**
- 杂粮：宽粉/苕粉/燕麦/高粱/紫米/荞麦/年糕/糯米粉
- 蔬菜：蒜苗/折耳根/豆芽/木耳菜/豌豆尖/魔芋/红薯/紫薯
- 荤腥：鸭血/毛肚/百叶/黄喉/郡肝/鹌鹑蛋/肥肠/卤牛肉/卤鸭/烤鸭
- 调料：面饼皮/干辣椒/泡椒/红油

**seed_dishes.py（+25 道新菜，去掉1个重复）**
- `_EXTRA_STAPLE`（7道）：粗粮饭/红豆薏米饭/紫米饭/荞麦饭/红薯蒸饭/高粱饭/豆花饭
- `_EXTRA_VEG`（6道，原7道去掉重复的干煸四季豆）：豆花/蒜苗回锅肉炒豆干/折耳根凉拌/魔芋烧鸭血/豌豆尖汤菜
- `_EXTRA_MEAT`（10道）：毛血旺/钵钵鸡/冒菜/川式卤拼/烤鸭/芋儿鸡/肥肠鱼/小炒黄牛肉/郡肝小炒/香辣虾
- `_EXTRA_SOUP`（3道）：酸萝卜老鸭汤/豌豆尖蛋汤/银耳红薯羹
- 通过 `DISH_SEED.extend()` 4 个列表追加到 DISH_SEED

**应季标签**（suit_solar 字段）
- 红豆薏米饭：谷雨-芒种
- 红薯蒸饭：霜降-大雪
- 豌豆尖汤菜/豌豆尖蛋汤：立春-惊蛰
- 银耳红薯羹：秋分-立冬
- 酸萝卜老鸭汤：立秋-秋分
- 毛血旺：立冬-大寒

### 关键 bug 修复
1. **食材名引用错误**：sed 批量修正 `藕` → `莲藕`、`蒜` → `大蒜`
2. **「干煸四季豆」重复**：原始 _VEG_DISH 和 _EXTRA_VEG 都有，删掉新加的，保留原始
3. **测试断言硬编码**：test_idempotent_and_counts 里 dish_main 80→105、ingredient 87→115

### 数据统计
- 菜品总数：80 → 105（+25）
- 食材总数：87 → 115（+28）
- 候选池：105 道（全部 region_type=1）
- 25 道新菜全部进入候选池
- 14 个日期模拟：54 道菜被选中，其中 14 道是新菜（含所有粗粮主食）

### 验证
- ✅ 语法检查通过
- ✅ 40/40 单元测试全绿
- ✅ 食材引用全部匹配
- ✅ 无重复菜名
- ✅ 新菜品在配餐结果中正常出现

## APK 桌面交付检查
- 用户要求将 APK 下载到桌面；已检查项目目录及 build 输出，当前未发现任何 `.apk` 文件。
- 由于远端 GitHub Actions workflow 仍未成功上传，当前没有可复制到桌面的 APK；未重复构建，也未使用此前暴露的凭证。

## APK 构建状态复查
- 2026-07-21 19:47 复查 GitHub Actions：workflow 已存在且 active，运行 #1 已 completed/success。
- 已生成 artifact `peican-apk`，约 63,340,488 bytes，未过期，预计 2026-08-20 到期。
- GitHub artifact 下载接口需要认证；当前未下载或复制到桌面。

## 国产在线构建平台调研
- 针对 GitHub 访问不稳定，完成国产在线 CI/CD 平台初步比较：优先云效 Flow，其次腾讯云 CODING，再考虑华为云 CodeArts Build；GitCode/AtomGit 可备选，Cloud Studio 更偏在线开发工作区。
- 当前 Flet APK 需验证 Python 3.11、JDK、Android SDK、Flutter、`pip install flet==0.86.1`、`flet build apk` 和 APK 制品下载；迁移后可绕开 GitHub，但通常要重新导入/镜像仓库及配置构建环境。

## 配餐规则增量开发
- 早餐改为粥/羹、杂粮、凉菜小菜优先，并排除重辣重油和毛血旺/冒菜/肥肠鱼等重口菜。
- 午晚餐增加节气匹配、主食/蛋白/蔬菜平衡、午晚餐主菜去重和近期方案惩罚；同日稳定、跨日轮换，候选不足自动兜底。
- 修改 `app/services/meal_planner.py`、`tests/test_core.py`；工程师 `IS_PASS: YES`，核心测试 47/47 通过，已知问题 0。

## 全局 UI 与菜单体验改版
- 采用用户提供的自然绿色色板：背景 `#F5F5F0`，主色 `#808C50`，深文字/深色栏 `#363C2B` 等，统一到 `app/theme.py` 与 `main.py`。
- 首页直入 `/home`，不再进入独立定位首屏；城市下拉入口放首页顶部左侧。结果页操作按钮支持换行，卡片、文字和按钮改为窄屏友好且提高对比度。
- 宴请方案的“重新配置”按 `plan.banquet` 返回 `/banquet`，不再回到普通就餐人员选择；增加相应测试。
- 配餐支持午晚餐面食主食时减少配菜但保持均衡；购物清单过滤主食和调味食材；菜谱说明提供可读兜底。修改 `app/config.py`、`app/theme.py`、`main.py`、`app/pages/home_page.py`、`app/pages/result_page.py`、`meal_planner.py`、`shopping_service.py`、`tests/test_core.py`。
- 核心标准库测试 51/51 通过；尚未进行真实手机尺寸截图/真机视觉复核，建议后续安装包真机验证。
- 第二轮 QA 后，购物过滤补充排除食用油、料酒、蚝油、鸡精、味精、胡椒粉、十三香、五香粉、孜然、香油/芝麻油；`diner_select_page.py`、`member_page.py`、`shopping_page.py` 去除 400/320/160 等固定宽度，支持窄屏换行。第二轮回归无源码问题需回流，智能路由判定 NoOne。

## 家常菜与网页凉菜融合
- 家常菜与网页凉菜数据去重融合后，种子库为175道菜、146种食材；新增凉菜包括皮蛋拌豆腐、麻酱豆角、夏日大拌菜、凉拌海蜇、凉拌毛豆、凉拌鸡丝等，购买型卤味明确标注招待小菜，海蜇/裙带菜标注脱盐处理。
- 种子审计确认菜名无重复、食材引用无缺失、结构字段完整。QA第二轮发现食材阈值误设为>=147，实际有效食材146；工程修正为>=146，核心测试恢复退出码0。
- 购物清单基础主食过滤新增糯米，明确与大米、面粉、面条一起不列入清单；该最后修复仅完成工程自检，因QA两轮额度已用尽未做第三轮独立复测。

## 家常菜谱与网页凉菜扩充
- 融合用户提供的家常菜数据：种子库达到 157 道菜、134 种食材；通过菜名去重、主材引用校验和字段完整性测试。
- 从用户给出的知乎凉菜页面筛选融合 17 道适合家庭配餐的凉菜，最终库为 175 道菜、146 种食材；新增皮蛋拌豆腐、麻酱豆角、夏日大拌菜、凉拌海蜇、凉拌毛豆、凉拌鸡丝、凉拌裙带菜、凉拌杏鲍菇等，并将卤味类标记为购买型招待小菜。
- 不重复已有凉拌黄瓜、木耳、海带丝、腐竹、秋葵、藕片等；海蜇/裙带菜添加泡发脱盐提示；调味料不作为购物主材。
- QA 两轮发现并定位：食材计数阈值应为 >=146（实际有效新增12种食材），以及糯米未被基础主食过滤；工程师已将阈值校正并在 `shopping_service.py` 将糯米加入排除集，同时在测试中覆盖糯米、米面与主要肉菜的保留行为。
- 最终工程自检：`py_compile` 与标准库核心测试退出码为 0。QA 两轮额度已用尽，因此糯米修复后的结果为工程自检，未进行第三轮独立 QA。

## Flet 0.86.1 API 兼容修复
- 修复用户运行 `flet run main.py` 时的旧 API 报错：`app/theme.py` 用 `ft.Border.all(1, COLOR_BORDER)` 替代 `ft.border.all`，主题按钮由 `ft.ElevatedButton(text=label)` 改为 `content=label`；`app/pages/home_page.py` 将 `ft.margin.only` 改为 `ft.Margin.only`。
- 全项目生产 Python 源码扫描无 `ft.border.all`、`flet.border.all` 或 `ft.Button(..., text=...)` 遗留调用；补充主题按钮兼容测试。
- 工程自检：py_compile 通过，标准库核心测试55项通过，11个注册路由视图可构造，未出现 border/Button/margin 相关错误。独立 QA 因协作额度耗尽未启动。

## 成员页与配餐操作修复
- 修复“添加家庭成员”在窄屏看似空白：`member_page.py` 移除固定宽度和不换行 Row，表单、按钮行、下拉和滑块改为弹性/换行布局；成员与新增/编辑路由均可构造且含内容。
- `diner_select_page.py` 的早餐/午餐/晚餐常规手机宽度单行显示，窄屏自动换行；`result_page.py` 改为原地“重新配餐”，新增 `rotation_seed` 保持原条件并主动改变同日候选排序。
- 结果操作固定2x2：“重新配餐 / 更替选菜 / 买菜清单 / 保存配餐”；详情页移除菜名至功效的冗余上半区，保留食材、做法、搭配、忌口和注意事项。
- 稳定测试：`python -X faulthandler tests/test_core.py` 运行58项，全部OK；受控路由 `/member`、`/member_edit`、`/diner_select`、`/result`、`/dish/1` 均可构造。

## 截图布局二次修复
- 成员编辑页灰色大块根因为口味偏好 Slider 在可滚动布局中 `expand=True` 撑满剩余高度；改为 `width=220` 紧凑 Slider，后续健康、忌口、素食及保存/取消控件可滚动显示。
- 餐次竖排根因为 Checkbox 自带 label 未显式横向组合；现为三个 `Row([Checkbox, Text])` 放入一个横向外层 Row，常规宽度同一行、极窄才整体换行，选中状态仍沿用原 Checkbox。
- 独立 QA 验证控件树、状态回调与 Flet API 扫描通过；稳定核心测试增至60项，全部OK，路由判定NoOne。

## 餐次单行与重配差异修复
- 用户反馈餐次仍竖排、早晚餐同菜、重新配置差异不明显。`diner_select_page.py` 改为直接横向放置 3 个原生 Checkbox，`wrap=False`、间距8，保留“早餐/午餐/晚餐”标签，强制单行。
- `meal_planner.generate_daily_meals()` 新增 `avoid_dish_ids`；维护全日已选菜 ID，保障早餐、午餐、晚餐之间不重复同一道菜。重配时先整体避开旧菜单，候选不足才允许复用旧菜，仍禁止本次跨餐重复。
- `result_page.py` 的“重新配餐”从上次 `_rotation_seed` 连续尝试后续8个 seed，并传入旧菜单 ID；成功后记录 seed 且提示“整体菜单已更新”，无法产生明显变化时保留旧结果并提示原因。
- 新增单行布局、跨餐去重、主动重配避旧菜单测试；`py_compile` 通过，完整核心测试 61/61 通过。
