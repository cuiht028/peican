# 2026-07-23 配餐工作日志

## GitHub Actions APK 构建
- 用户在本机安全完成 Git rebase 与正常推送，未使用强推；最新代码已同步至 GitHub main。
- GitHub Actions `Build Android APK` 成功完成：run `29976509322`，提交 `66a97756ddb46f4dec9414b3cb76a2674fe332b9`。
- 制品 `peican-apk` 可下载，大小 63,414,607 bytes，SHA-256 `9f6a3e2fbe119c44532cc24da67735ad01e4a374fad74449200d38cbd90bd115`，有效至 2026-08-22。

## 配餐系统大改版（收尾修复）
- **早餐专用菜谱库**：新增 dish_type=5，包含白粥、八宝粥、豆浆、包子、馒头、油条、煎饼果子等早餐经典，避免重口主菜。
- **菜品扩充**：新增咖喱鸡、冒血毛、椒盐排骨、水煮牛肉等菜品；三色彩椒炒肉、白灼虾、干锅系列等家常菜。
- **分类修正**：酸辣粉改为 type=6（其他），不再作为主食。
- **早餐结构修复**：`get_meal_structure` 早餐改为 `{5:1, 2:1}`（1 道 type=5 + 1 道素菜），并已归一化含 key 1..5。
- **早餐选菜 bug 修复**：原 `meal_planner.generate_daily_meals` 早餐分支用 `sum(1,2,3,4)` 计算总数导致漏掉 type=5，早餐只选 1 道、素菜补不进；改为按 `structure.items()` 逐类填充。
- **早餐禁用名单**：显式排除 `豆花饭 / 皮蛋拌豆腐 / 凉拌猪耳朵`（用户点名不宜早餐出现）。
- **种子库去重**：移除大改版引入的 6 道重名菜（鱼香茄子、糖醋里脊、水煮鱼、酸菜鱼、辣子鱼块、凉拌三丝），总数 214→208。
- **重新配餐**：`avoid_dish_ids` 进入候选排除集 + 分数降权，重配与上次零重合，确有明显变化。
- **测试验证**：`python -m unittest tests.test_core` 61/61 全绿。